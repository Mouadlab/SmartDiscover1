services:
  - type: web
    name: smartdiscover
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout=120 --workers=2 auth:application
    envVars:
      - key: GEMINI_API_KEY
        sync: false   # à renseigner manuellement dans le dashboard Render
